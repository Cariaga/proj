version: "3.7"
services:
  web:
    build: .
    command: npm start
    
    networks:
      - someApp
    volumes:
      - .:/usr/src/app
    ports:
      - "8081:8080"
    deploy:
      replicas: 51
      resources:
        limits:
          cpus: "0.1"
          memory: '50M'
      restart_policy:
        condition: on-failure
    networks:
      - webnet
  web:
    build: .
    command: npm start
    
    networks:
      - someApp
    volumes:
      - .:/usr/src/app
    ports:
      - "8082:8080"
    deploy:
      replicas: 51
      resources:
        limits:
          cpus: "0.1"
          memory: '50M'
      restart_policy:
        condition: on-failure
    networks:
      - webnet
networks:
  webnet: